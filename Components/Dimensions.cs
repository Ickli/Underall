namespace Underall.Components;


/// <summary>
/// Enum dictating order in which dimension-related fields of components must be arranged
/// </summary>
public enum Dimensions
{
    Real,
    Imaginary,
}